{
  "name": "raqamli-mahalla-root",
  "version": "1.0.0",
  "description": "Raqamli Mahalla Full-Stack Workspace",
  "scripts": {
    "dev": "npx concurrently -k -p \"[{name}] text\" -n \"BACKEND,FRONTEND\" -c \"green,blue\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "test": "npm --prefix backend run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
